[package]
name = "erc721"
version = "0.1.0"

[dependencies]
starknet = ">=2.5.3"

[dev-dependencies]
snforge_std = { git = "https://github.com/foundry-rs/starknet-foundry", tag = "v0.18.0" }

[scripts]
test = "snforge test"


[[target.starknet-contract]]
sierra = true
casm = true
